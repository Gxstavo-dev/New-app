// este componente es el area de contenido
// ahora mismo esta vacio, solo reserva espacio
// aqui se va a dibujar el editor o la vista de cada nota

export default function ContentBox() {
  // una seccion que ocupa todo el espacio disponible (flex-1)
  return <section className="h-full flex-1 "></section>;
}
